'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fff',
      contrastText: '#1976d2',
    },
    secondary: {
      main: '#1976d2',
      contrastText: '#1976d2',
    },
  },

});

export default theme;
